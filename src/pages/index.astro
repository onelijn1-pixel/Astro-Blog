---
import Layout from "../layouts/Layout.astro";
import ListPost from "../components/ListPost.astro";
import AboutSummary from "../components/AboutSummary.astro";

import { getCollection } from "astro:content";
const posts = await getCollection("blog");
---

<Layout>
  <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
    <section class="mb-16" id="posts">
      <h2
        class="text-3xl font-bold text-gray-900 mb-8 border-l-4 border-indigo-600 pl-3"
      >
        📚 Các Bài Viết
      </h2>

      <div class="grid grid-cols-1 lg:grid-cols-4 gap-10">
        <div class="lg:col-span-3 space-y-10" id="posts">
          <ListPost />
        </div>
        <aside class="lg:col-span-1 space-y-8">
          <AboutSummary />

          <div class="bg-white p-6 rounded-xl border border-gray-200">
            <h4 class="text-xl font-bold mb-4 border-b pb-2">Danh Mục</h4>
            <ul class="space-y-2">
              <li>
                <a
                  href="#"
                  aria-label="#LậpTrình"
                  class="text-gray-800 hover:text-astro-purple flex justify-between"
                  ><span>#LậpTrình</span>
                  <span class="text-xs text-gray-600"
                    >(<span>{posts.length}</span>)</span
                  ></a
                >
              </li>
            </ul>
          </div>
        </aside>
      </div>
    </section>
  </main>
</Layout>
